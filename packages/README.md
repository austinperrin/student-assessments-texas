# Packages

This folder is reserved for future shared libraries, schemas, and reusable
tooling modules.

Possible future uses:

- shared assessment loaders rooted on `assessments/`
- shared mapping parsers
- schema or contract packages
- reusable validation helpers
- utilities shared by future services
