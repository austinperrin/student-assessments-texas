# Script Libraries

This folder is reserved for shared helper modules used by multiple scripts.

Add reusable Python or shell helpers here when a script behavior is shared
across categories such as `ci/`, `docs/`, or `mappings/`.
