# Docs Scripts

This folder is reserved for documentation maintenance helpers.

Use this area for scripts that support repository docs, such as link audits,
index generation, or future documentation consistency checks that are not CI entrypoints themselves.
